FROM python:3.11-slim

RUN apt-get update && apt-get install -y \
    ocrmypdf tesseract-ocr tesseract-ocr-ita tesseract-ocr-eng poppler-utils qpdf \
  && rm -rf /var/lib/apt/lists/*

# No entrypoint: used as a toolbox image to run ocrmypdf with --network none


